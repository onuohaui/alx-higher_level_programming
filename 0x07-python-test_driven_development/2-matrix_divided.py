#!/usr/bin/python3
def matrix_divided(matrix, div):
    """Function that divides all elements of a matrix.
    Args:
        matrix (list of lists of ints or floats): The matrix to be divided.
        div (int or float): The divisor.
    Returns:
        list: New matrix with divided elements.
    Raises:
        TypeError: If the matrix is not a list of lists of integers/floats, or
                   if each row of the matrix is not the same size, or
                   if div is not a number.
        ZeroDivisionError: If div is 0.
    """
    # Your implementation here
    pass
